<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.HIFA" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_no_bg</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.HIFA</item>
    </style>

    <style name="Theme.HIFA" parent="Base.Theme.HIFA" />
</resources>